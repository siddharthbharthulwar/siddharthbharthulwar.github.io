import React from 'react'

const School = ( { school } ) => {

   /* var image = require(`../Img/${school.imgFileName}`)*/

    return (
        <div>
            <h1>{school.name}</h1>
            
        </div>
    )
}

export default School
