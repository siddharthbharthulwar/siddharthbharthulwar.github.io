import React from 'react'

const School = ( { school } ) => {

    var image = require(`../Img/${school.imgFileName}`)

    return (
        <div>
            
        </div>
    )
}

export default School
